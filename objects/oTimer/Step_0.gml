// Decrease the countdown time each frame
countdown_time -= 1;

// Check if the countdown has reached zero
if (countdown_time <= 0) {
    // Perform actions when the timer reaches zero
    // For example, end the game or trigger an event
}
